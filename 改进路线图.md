# ☕ 咖啡笔记改进路线图

> 状态核实日期：2026-08-14（当前版本 v2.9.3，versionCode 88，database v21）
> 依据：`项目审计报告.md`（2026-07-27，v2.7.0）+ AGENTS.md 技术债清单 + 本次代码核实
> 原则：按「改到相关文件时顺手修」推进，不集中发版；优先级从上到下。

---

## 〇、现状核实：审计报告哪些已还、哪些没还

### ✅ 已处理（审计后到 v2.9.3 之间修复的）

| 审计问题 | 当前状态 |
|---------|---------|
| extractionMethod 迁移遗漏 | 已修（现库 v21，schemas/20.json、21.json 已提交） |
| fallbackToDestructiveMigration 静默清库 | 已收紧为 `fallbackToDestructiveMigrationOnDowngrade()`（AppDatabase.kt:66） |
| exportSchema = false | 已开 `exportSchema = true`（AppDatabase.kt:35）+ schema JSON 入仓库 |
| importBackup 无事务 | 已修 |
| Flow collect 泄漏 / 重像素运算 / 备份资源关闭 | 已修（AGENTS.md 注明不再列） |
| 依赖升级 | CameraX 1.5.0、Coil 3.1.0、lifecycle 2.9.0 均已到位 |
| release 混淆 | 已开 `isMinifyEnabled` + `isShrinkResources` |

### ❌ 仍未处理（以下路线图的主体）

---

## 一、第一梯队：数据安全（血泪教训区，最优先）

### 1.1 Room Migration 全链路测试 ✅ 已完成（2026-08-15）
- **结果**：`app/src/androidTest/java/com/coffeelab/coffeenotes/data/MigrationTest.kt` 落地，v20→v21 迁移测试在真机跑通（BUILD SUCCESSFUL）。
- **重大收获**：测试首次运行即抓到 **MIGRATION_20_21 的 SQL 与 Room 实体不一致**——迁移 SQL 多写了 `FOREIGN KEY(beanId) REFERENCES coffee_beans(id) ON DELETE CASCADE`，而 `StockAdjustment` 实体未声明外键（21.json 无 FK），导致 `Migration didn't properly handle: stock_adjustments`。已修正迁移 SQL 去掉 FK 子句（对已升级用户无影响，迁移只跑一次）。
- **教训**：迁移 SQL 必须与实体定义完全一致（外键/默认值/列序），任何偏差都会被 MigrationTestHelper 严格校验抓出——这就是装机前的防线。
- **未覆盖**：schemas 仅 20/21.json，早期 1~19 的 schema JSON 未入仓库，1→20 各段暂无法测。后续可从历史版本导出 schema JSON 补全，再扩展为全链路。
- **配套改动**：`app/build.gradle.kts` 加 `room-testing:2.7.1` 依赖 + `androidTest.assets` 指向 `app/schemas`；`MIGRATION_20_21` 可见性 private → internal。
- **⚠️ 跑 androidTest 的安全姿势**：`connectedDebugAndroidTest` 跑完会自动卸载 app 清空数据（2026-08-15 事故），必须用「`assembleDebugAndroidTest` + `adb install -r` ×2 + `adb shell am instrument -w`」手动流程（详见 AGENTS.md 装机铁律）。
- **工作量**：半天（实际约 1.5 小时含事故处理）。需要 `androidTestImplementation("androidx.room:room-testing:2.7.1")`。

### 1.2 云端备份（产品级数据安全）✅ 已拍板：备份保持明文
- **用户决策（2026-08-14）**：备份保持明文 ZIP + `allowBackup=true`，数据不敏感，明文方便导出查看，**不做加密**。故原「备份加密」条目从技术债清单移除，勿再建议。
- **动机**：现在备份是"导出 ZIP 到本地 /sdcard"，手机丢了 = 全部数据没了。这是 App 目前最大的真实痛点。
- **做法**：加 WebDAV（坚果云/Nextcloud）或飞书云盘通道，设置页配地址+凭据，复用现有备份提醒闹钟做"每周自动上传"。备份格式已有版本兼容机制，落地不难。明文上云若介意第三方可见，可留给用户自行选择网盘（个人网盘默认私有，风险可控）。
- **工作量**：2~3 天。

---

## 二、第二梯队：构建与发布（半天做完，收益立竿见影）

### 2.1 release signingConfig
- **位置**：`app/build.gradle.kts:25-34`（release buildType 无 signingConfig，产出 unsigned）
- **做法**：keystore 路径/密码走环境变量或 local.properties（**不入仓库**），配好后 `./gradlew assembleRelease` 一条命令出可安装包。
- **工作量**：半小时。

### 2.2 gradle 构建提速 ✅ 已完成（2026-08-15）
- **位置**：`gradle.properties`（当前无 caching/parallel）
- **做法**：加 `org.gradle.caching=true` + `org.gradle.parallel=true`（已加，构建验证通过）。
- **工作量**：一分钟，纯白捡。

### 2.3 GitHub Actions CI
- **动机**：项目在 GitHub（jimmywuxin/coffee-notes），零成本自动化。
- **做法**：`.github/workflows/ci.yml`：push 跑 `./gradlew test assembleDebug`；打 tag（如 `v2.9.3`）时 `assembleRelease`（需先配好 2.1 的签名，密码走 repo secrets）并把 APK 挂到 GitHub Releases——以后装机/升级直接下 Releases，不再人肉 adb。
- **工作量**：半天。

---

## 三、第三梯队：性能（改到相关文件时顺手修）

| # | 位置 | 问题 | 做法 |
|---|------|------|------|
| 3.1 | 全项目 | ~~`collectAsState` 未用 `collectAsStateWithLifecycle`，后台仍采集 Flow~~ ✅ 已做（2026-08-15）：17 文件 ~70 处替换，无参调用补 `initialValue = flow.value`；含 3.5 一并完成 | 依赖已在（lifecycle-runtime-compose 2.9.0） |
| 3.2 | `ImageUtils.kt:43` | ~~`decodeStream` 无 `inSampleSize` 预采样，大图 OOM~~ ✅ 已做（2026-08-15）：`inJustDecodeBounds` 读尺寸 → `calculateInSampleSize` → 带采样解码后再精确缩放 | 先 `inJustDecodeBounds` 读尺寸算采样率，再解码 |
| 3.3 | `BrewListScreen.kt:330` | ~~`beans.find{...}` 线性查找 O(n×m)~~ ✅ 已做（2026-08-15）：`beansById = remember(beans) { beans.associateBy { it.id } }`，查找 O(1) | 预构建 `Map<Long, CoffeeBean>`，O(n) |
| 3.4 | `CoffeeBeanDao.kt:37-53` | `searchBeansFull` 10 列 LIKE + 3 表 JOIN 全表扫描 | 数据量大再说，FTS 是终极方案，暂缓 |
| 3.5 | `StatsScreen.kt` 多处 | ~~每次重组新建 Flow 订阅~~ ✅ 已做（2026-08-15）：4 处冷 Flow 调用包 `remember(beanId)` / `remember` | `remember(beanId)` |

---

## 四、第四梯队：代码质量（债，不急）

| # | 位置 | 问题 | 做法 |
|---|------|------|------|
| 4.1 | `BeanViewModel.kt`（309 行）/ `BackupViewModel.kt`（661 行） | God Class | 按职责拆分（豆子/标签/OCR/图片/备份导出/备份导入） |
| 4.2 | 6 个管理屏（Equipment/Grinder/ImpressionTag/ProcessMethod/RoastDegree/BrewMethod） | 拖拽排序+删除弹窗复制粘贴 6 份 | 抽可复用组件（已有 DraggableItem 可作基础） |
| 4.3 | 全项目 | 硬编码中文文案 873 处，strings.xml 仅 app_name 一个 string | 工程量大；不做国际化则优先级垫底，仅在做改动时顺手抽公共文案 |
| 4.4 | `BrewEditScreen.kt:164` | 注释承诺"手改不覆盖 waterAmount"，代码无此判断 | 补上判断 |
| 4.5 | `AppDatabaseMigrations.kt` MIGRATION_15_16 | 加 equipmentId/grinderId 未 DROP 旧 String 列 | 顺手加 DROP（新迁移，勿改已发布迁移） |
| 4.6 | `AndroidManifest.xml:6` | INTERNET 权限可能冗余（OCR 端侧 ML Kit） | 若不上云端备份可移除；做了 1.2 则保留 |
| 4.7 | 杂项 | `!!` 强解引用、魔法数字（`86400000L`/`15`天/`28`天/`1600`px/`60f`/`200`ms） | 顺手抽常量/空安全 |

---

## 五、第五梯队：产品功能（"做得更好"的上限在这）

1. **分享精美冲煮卡片**：一次冲煮记录渲染成卡片图导出分享（朋友圈/小红书），咖啡类 App 最自然的传播点。Compose 可以 `GraphicsLayer` 转 Bitmap 再分享。
2. **月度/年度回顾**：类似 Spotify Wrapped——"本月喝了 X 杯、最喜欢的豆子、风味词云、评分趋势"，统计基础设施已有（StatsScreen），实现不难，自用也满足。
3. **赏味期提醒**：备份提醒的闹钟机制现成，复用做"豆子进入赏味期/快过期"通知。
4. **README 版本同步**：README 还写 2.6.1，实际 2.9.3，顺手改。

---

## 六、推荐执行顺序

**下个周末清单（约 2 天）**：
1. 1.1 Migration 全链路测试（数据安全兜底，半天）
2. 2.1 + 2.2 + 2.3 构建发布三件套（半天）
3. 3.1 collectAsStateWithLifecycle 全量替换（半天，含验证）

**随后（每周末一项）**：
4. 1.2 云端备份（数据安全闭环，2~3 天）
5. 3.2/3.3/3.5 顺手性能项
6. 4.1/4.2 代码质量重构
7. 5.x 产品功能按兴趣选做
